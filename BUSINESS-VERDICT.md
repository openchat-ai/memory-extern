# 商业可行性结项结论（2026-08）

> 本文件是"个人电脑专家缓存 / 外置推理加速"方向的**最终商业评估记录**。
> 结论：**商业价值不成立，不开公司、不外售**。本仓库保留为技术/RTL 与验证资产。
> 附录 A（下方）含本轮量化验证的完整账本，全部数字可复现。

---

## 0. 一句话结论

**一个 1 tile（791 cells）的芯片，证明一个大厂已经免费实现的能力——作为产品卖不动。
本方向从商业角度结项；从技术/学习/求职角度保留。**

---

## 1. 本方向的演进与终局

| 阶段 | 目标 | 结果 |
|------|------|------|
| RTL 版本 A（periph_mac） | fp32 矩阵外围芯片 | 用户决定非流片对象，退役 |
| RTL 版本 B（tt_um_bf16） | bf16 跨组累加演示，1 tile | 791 cells / 41 FF，提交 `1d2045b`，11 门回归全绿 |
| 产品形态调查 | 千万 i5/i7 + 16GB 无显卡电脑跑 LLM ≥50 t/s | 见 §2，物理上不可达 |
| 商业评估 | 贷款开公司孵化 | **否决，见 §3** |

---

## 2. 为什么"个人电脑 + LLM + 加速芯片"成立不了（量化账本）

### 2.1 目标模型本身不匹配

- Qwen3.8-27B：**dense**（64 层全是全量激活），无"热专家"可缓存 → 缓存技术零用武之地；
  且 int4 权重 17GB 装不进 16GB 机器。
- Qwen3-30B-A3B（MoE，128 专家取 8）：缓存技术唯一成立对象，但 **Q4_K_M=18.6GB** →
  需要 **32GB 内存**——而 32GB 在 2026 装机市场已是**新机默认标配**（5000$ 档起 2×16GB，
  国产套装 ~900 元）。
- **双反锁**：16GB 机器装不下能缓存的 MoE；能缓存的 MoE 只在自带 32GB 的新机上跑，
  其中 33% 用户已能获得软件级 40-70 t/s。

### 2.2 目标机器的物理死结

| 平台 | 内存通道 | 有效带宽 | 装 30B-A3B Q4 |
|------|---------|---------|---------------|
| 16GB 老笔记本（i5/i7） | 双通道 DDR4 | ~26-32 GB/s | **装不下（18.6GB）** |
| 32GB 台式（i5/i7） | 双通道 DDR5 | ~40-60 GB/s | ✅（需压 ctx/cache） |
| 工作站（8 通道 RDIMM） | 8ch | ~335 GB/s | ✅ **无需缓存已 200+ t/s** |

- 加内存只加容量，**不加带宽**（通道数物理固定）——没有"插好内存就跑 50 t/s"这回事。
- HBM 无法插入任何主板（TSV+中介层共封装，非 DIMM）——dense 路线的 700 GB/s 需求
  不是消费者可及对象。

### 2.3 命中率机制的量化（已跑通，可复现）

- `sim_cache2.py / report_real.py`（K3 结构 trace，100,096 请求 / 10,010 distinct）：
  LRU 原序 **36.24%** → 层优先重排 **90.00%**（=理论上限），仅需 8GB 容量。
  同一集合/数量/策略，**只改调度顺序**——顺序是第一决定因素。
- `<30B-A3B 实测对标（sim_compare.py，社区真实 TG 基线）>`

### 2.4 软件 vs 硬件差距（真实数字）

前提：llama.cpp 今天**不做跨 token 专家权重缓存**；你的软件把专家 LRU 缓存实现后：

| 实现 | 每 token 搬运 | vs llama.cpp | 带宽墙（DDR5 双通道） |
|------|--------------|--------------|----------------------|
| llama.cpp（现状） | 1.60 GB | 1.0x | ~25-37 t/s |
| 你的软件（36% 命中） | 1.28 GB | **1.25x** | ~31-47 t/s |
| 你的芯片（90% 命中） | 0.79 GB | **2.1x** | ~50-76 t/s |

**残酷点**：芯片的 2.1x 只有**同时解掉算力墙**才拿得到（CPU-only 下单缓存，被算力墙
压死，富余带宽放不出来）。解算力墙 = GEMM 引擎 = 掉回 AMD/NVIDIA 的主场
（HBM + TFLOPS）。纯缓存的芯片增量在 CPU-only 下约等于零。

**结论**：你独立的、能卖出去的性能物理指标不存在。

### 2.5 竞品免费占据

| "能力" | 谁免费提供 |
|--------|-----------|
| 专家缓存 / slot 分页 | llama.cpp mmap + 内核缺页；SparkMoE 等开源工程 |
| 时序预取 | anemll Flash-MoE 等开源 |
| 40-200 t/s | 工作站 8 通道内存 335GB/s；Mac 统一内存 300+GB/s |
| 近存/缓存 | 大厂：HBM+CoWoS+数据中心 |

---

## 3. 商业结论

- **不贷款、不开公司、不售卡**。目标人群（个人电脑推理）已被软件与工作站双向封死。
- **诚实定位**：本仓库是"缓存/权重搬运研究 + RTL 教学 + 真机验证"资产；
  结项动作不含"继续做产品化"。
- **沉淀资产**（继续有效，去向=简历/求职/技术积累）：
  1. TT 流片级 RTL：版本 B（791 cells, 1 tile）+ 11 门回归 + opt.sh/check.sh。
  2. 完整可复现的命中率/带宽分析链（sim_cache2, report_real, sim_compare, run_true_machine）。
  3. **判断力**：用真数据提前否决一个商业方向，成本远低于踩上去；这是稀缺能力。

---

## 4. 自用转向（非商业方向，研究资产复用）

> 商业结论不变：**不卖**。但本卷积淀的工具链转身为个人用途——**"token 自由"**：
> 在 32GB 无显卡机器上，用开源 MoE 跑出 API 级能力且不花钱。

- **分工定位**：
  - `Qwen3-30B-A3B-Instruct-2507`（MoE，Q4_K_M=18.6GB）→ **交互主力**（写代码/对话，12-30 t/s）。
  - `Qwen3.8-27B`（dense，已下载 Q3）→ 不自欺：单流被带宽墙焊在 ~4 t/s，**不适合做交互**。
    它的正确用法是**并发批处理**：权重共享，吞吐随 npl 摊薄（llama-batched-bench 实测确认）。
- **dense 27B 三档实测脚本** `tools/bench_dense_27b.sh`（2026 llama.cpp 命名）：
  1. 单流基线（llama-bench tg）
  2. MTP 投机解码（`--spec-type draft-mtp --spec-draft-n-max 2`，27B 自带 MTP 头免 draft 模型）
  3. 并发批处理（llama-batched-bench -npl 1,2,4,8）
  - 判断：单流不会突破带宽墙；`S t/s` 随 npl 上扬 = 自动补全/脚本批处理是 27B 的正路。
- 意向：**不是产品，是自用**。此条与 §3 商业结论不冲突。

---

## 5. 附录 A：可复现命令

```bash
python3 tools/sim_cache2.py data/expert_trace.bin           # 36.24% vs 90% (机制)
python3 tools/report_real.py                                 # trace 画像（736 runs, 10,010 distinct）
python3 tools/sim_compare.py --cpu-model ryzen7-ddr5        # 30B-A3B TG 对标（默认预设）
T_SOFT=22 python3 tools/sim_compare.py --soft 22 --bw 40    # 真机实测回填
bash tools/run_true_machine.sh                              # 真机: 装 llama.cpp → 测 27B + 30B-A3B 双模型
bash tools/bench_dense_27b.sh                               # dense 27B 三档实测（基线/MTP/并发）
MODEL_27B=/path/27b.gguf bash tools/bench_dense_27b.sh      # 手动指定 27B 路径
```

真机回填判定（重跑 sim_compare 后看现实可达列）：
- T_SOFT ≥ 30 → 带宽主瓶颈 → 你软件的 +25% 兑现
- T_SOFT < 30  → 算力墙主导 → 芯片必须连算力一起解才卖得出——否定中的否定

**已回填定案（2026-08-18，真机拔条实测 + sim_compare 最终对账）**：
- 实测 `T_SOFT = 5.58`（32GB 2×16@3200MHz，35B-A3B Q3_K_S）、`bw = 23`（dense 折算）
- **`T_SOFT ≪ 带宽墙_B`（5.58 vs 18.0，~3 倍裕量）→ 本机算力墙主导**，软件远未触带宽
- **芯片纯缓存增量 = 0 t/s**；唯一出路 = "芯片连算力一起解"（卸载专家 GEMM）
- 与拔条实验互证：2400→3200MT/s 只涨 20%（dense）/9%（MoE），带宽富余放不出来
- 附录 §2 的"算力墙必须连算力一起解"由假设升级为**实测结论**

---

> 结项。此文档随版本 B 一并归档，仓库不再新增商业方向内容。