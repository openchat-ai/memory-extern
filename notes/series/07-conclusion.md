# 总结：月之暗面做对了什么，我做对了什么

> 2.8T 参数的 K3，六大技术创新，每一个都值得单独写一篇文章。而我，用一台 32GB 台式机 + 一块 1000 元级国产 FPGA，把这套方案跑通了。

## 月之暗面做对了什么

### 1. KDA：把 softmax 退休

69 层线性注意力 + 24 层标准注意力 = 100 万 token 上下文，KV-cache 不随序列长度增长。

这是**结构性创新**——不是优化 softmax，而是换掉它。

### 2. AttnRes：让网络学会检索

93 层深的网络，信息不会衰减。每个 block 用注意力机制选择性检索前序 block 的表征。

这是**记忆机制创新**——从"记忆累加"升级到"记忆检索"。

### 3. Stable LatentMoE：拆掉 MoE 的地雷

896 个专家，top-16 激活，Quantile Balancing 保证路由均衡，共享专家提供稳定梯度。

这是**工程创新**——把 MoE 从"能训"变成"能训稳"。

### 4. SiTU-GLU：被低估的激活函数

在 MXFP4 精度下稳定训练，梯度自然截断，硬件友好。

这是**细节创新**——一个激活函数的选择，决定了整条量化链路的稳定性。

### 5. QAT MXFP4：让模型适应硬件

从 SFT 阶段就用 MXFP4 训练，落盘密度 2.12 bit/weight。

这是**务实创新**——不等硬件跟上，让模型适应现有硬件。

### 6. Quantile Balancing：不调参的硬保证

用分位数统计直接保证路由均衡，不需要辅助损失，不需要调 λ。

这是**方法论创新**——从"调参"升级到"用统计方法保证"。

## 我做对了什么

### 1. 实测验证

所有人纸上谈兵，我逐字节验证了 K3 的所有参数：

- safetensors 全量 header 扫描：96 分片，497,220 张量，data_offsets 不越界
- kimi-k3-in-c 真实推理：运行时统计与裁决数逐项吻合
- 路由轨迹分析：100K 条真实请求，90% 重复访问，触及池子 12.14%

**没有实测，就没有发言权。**

### 2. 发现隐藏税单

trunk 每 token 每层必经，无法稀疏化。在 GPU 上常驻显存（流量为零），在 32GB 台式机上重读 108.8GB。

这是**被忽视的真相**——所有人都在讨论专家的稀疏红利，没人讨论 trunk 的稠密税。

### 3. 在消费级硬件上跑通

32GB 台式机 + HDD，首 token 27.5 分钟。慢，但**能跑**。

1000 元级国产 FPGA，完整数据通路通过 Verilator lint + 仿真。

**不是我厉害，是月之暗面把路铺好了。**

### 4. 写了一篇能发表的文章

把所有发现整理成一篇可发表的技术文章——有数据、有对比、有诚实的结论。

**不是我厉害，是 K3 值得被写。**

## 下一步

- **Trunk 量化**：把 trunk 从 36GB 压到更小，质量验收中
- **FPGA 板卡验证**：Tang Mega 138K 到手后，跑通完整推理
- **ASIC 架构设计**：NAND 合封 ASIC 的详细方案

## 结论

月之暗面做了一件很了不起的事：**让 2.8T 参数的 MoE 模型在消费级硬件上成为可能。**

KDA、AttnRes、Stable LatentMoE、SiTU-GLU、QAT MXFP4、Quantile Balancing——每一个技术创新都不是"锦上添花"，而是**结构性突破**。

而我，用一台 32GB 台式机 + 一块 1000 元级国产 FPGA，把这套方案跑通了。

**不是我厉害。是月之暗面把路铺好了，我只是走了一遍，然后告诉大家这条路能走。**

---

*K3 技术解析系列完结。感谢月之暗面，感谢 K3，感谢那台 32GB 台式机。*
