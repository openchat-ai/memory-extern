# LLM 推理芯片项目计划

> 2026-08-20 定稿。
>
> **核心洞察**：LLM 推理是内存墙问题，不是算力问题。谁的内存带宽高，谁就赢。
>
> **产品策略**：同一块 PCB，高低配两代芯片。
> - **V1（先做）**：纯数字 GEMV 加速卡，全直焊 LPDDR5X，能活。
> - **V2（后做）**：同一 PCB，焊上 CIM 芯片，性能突破。

## 产品定义

| 项 | V1（低配） | V2（高配） |
|---|---|---|
| 产品 | PCIe 加速卡 | PCIe 加速卡（+CIM） |
| 主芯片 | GEMV 阵列 + 内存控制器 | 同 V1 |
| CIM 芯片 | **空焊盘（不贴片）** | **焊上 CIM 芯片** |
| 内存 | **全直焊 LPDDR5X**（12 颗） | 同 V1 |
| 工艺 | **14nm FinFET（中芯国际）** | 14nm |
| 功耗 | ~40W | ~55W（+15W CIM） |
| 带宽 | **550 GB/s** | 更高（CIM 加速） |
| 容量 | 24GB / 48GB / 96GB（买时选） | 同 V1 |
| 成本 | ~$250 | ~$350（+CIM 芯片成本） |
| 目标 tok/s | 25-30（7B bf16） | 50-100+ |

## 范围外（不做）

| 工作 | 原因 |
|------|------|
| PIM-DIMM 方案 | 已证伪：做不过 GPU，最终变成 GPU |
| SRAM CIM 阵列（独立模块） | V2 做，V1 不做 |
| CIM 阵列 RTL（DAC→SRAM→ADC） | 属于 V2 CIM 芯片，不在 V1 主芯片内 |
| CPU 端 GEMV 内核优化（NEON/AVX2） | ggml 的活，不是芯片的活 |
| MXFP4 E2M1 引擎 | 真实权重精度不够（p50(max_err)=31%，DAC/ADC 不叠加），已证伪 |
| Q3/Q4 CPU benchmark | 芯片不做 CPU 侧优化 |
| TT 芯片提交 | 无商业价值 |
| 三级预取调度器（sched3/sched4） | 属于缓存架构，芯片不做缓存 |
| 缓存命中率分析 | 软件层问题，芯片不解决 |
| Trace 采集与分析 | 用于验证，不是芯片本身 |
| 芯片间互连/光互连 | 不需要，单芯片足够 |
| 3D 堆叠 | 用不起，成本太高 |
| HBM | 买不到，全给 NVIDIA/AMD 了 |
| GDDR6X | Micron 独家，正在停产 |
| SOCAMM2 插槽 | 带宽掉 3 倍（136 vs 460 GB/s），不值得 |
| 混合直焊+插槽 | 调度复杂，慢的拖累快的 |
| CXL + DDR5 | 带宽太低（~100 GB/s），不够用 |
| GDDR7（V1） | 功耗高、散热难，V2 再考虑 |
| 测试控制器（BIST） | V1 不需要，FPGA 验证够用 |
| profiling 工具 | V1 不需要，量产后再做 |

## 为什么全直焊，不用插槽

| | 全直焊 LPDDR5X | SOCAMM2 插槽 |
|---|---|---|
| 带宽 | **460 GB/s** | 136 GB/s（差 3 倍） |
| 延迟 | **低**（走线短） | 高（模块化损失） |
| 信号质量 | **好** | 差 |
| 可升级 | 不能 | 能 |
| 复杂度 | **低** | 高（两种速度管理） |
| 成本 | **低** | 高（插槽贵） |

**结论**：插槽带宽掉 3 倍，不值得。全直焊最简单、最快、最便宜。

## PCB 架构（高低配共用）

```
┌──────────────────────────────────────────────┐
│                加速卡 PCB                     │
│                                              │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ │
│  │DRAM│ │DRAM│ │DRAM│ │DRAM│ │DRAM│ │DRAM│ │
│  └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ │
│  │DRAM│ │DRAM│ │DRAM│ │DRAM│ │DRAM│ │DRAM│ │
│  └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ │
│     │      │      │      │      │      │    │
│  ┌──┴──────┴──────┴──────┴──────┴──────┴──┐ │
│  │     LPDDR5X 内存总线（460 GB/s）        │ │
│  └──────────────────┬─────────────────────┘ │
│                     │                        │
│  ┌──────────────────┴─────────────────────┐ │
│  │  主芯片（GEMV 阵列 + 内存控制器 +      │ │
│  │        PCIe + 片上 SRAM 2MB）          │ │
│  │        14nm FinFET，~15 mm²            │ │
│  └──────────────────┬─────────────────────┘ │
│                     │                        │
│  ┌──────────────────┴─────────────────────┐ │
│  │  [空焊盘] ← V2 在此焊上 CIM 芯片       │ │
│  │  （预留电源 + 信号走线）                 │ │
│  └──────────────────┬─────────────────────┘ │
│                     │                        │
│  ┌──────────────────┴─────────────────────┐ │
│  │  PCIe Gen4 x16 金手指                  │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘

V1：12 颗直焊 LPDDR5X + CIM 空焊盘，纯 GEMV 加速卡
V2：同一 PCB，CIM 焊盘贴片，GEMV + CIM 混合计算
```

## 内存配置（买时选）

| 型号 | DRAM 数量 | 容量 | 带宽 | 适用模型 |
|------|----------|------|------|---------|
| V1-24G | 12 × 16Gb | 24 GB | 460 GB/s | 7B bf16 / 13B int4 |
| V1-48G | 12 × 32Gb | 48 GB | 460 GB/s | 13B bf16 / 30B int4 |
| V1-96G | 12 × 64Gb | 96 GB | 460 GB/s | 30B bf16 / 70B int4 |

**推荐首选**：V1-48G（覆盖大部分场景）

**内存供应商**：
- 首选：长鑫 CXMT（国产，成本最低，供货最稳）
- 备选：Samsung / SK Hynix / Micron（LPDDR5X，10667 Mbps）

## 芯片设计（两颗独立 die）

### Die 1：主芯片（先流片）

```
┌──────────────────────────────────┐
│  LPDDR5X 内存控制器（8 通道）     │
│  ↓                               │
│  GEMV 计算阵列（115 bf16 MAC）   │
│  + 片上 SRAM（2MB）              │
│  ↓                               │
│  CIM 接口（预留，V1 不连）        │
│  ↓                               │
│  PCIe Gen4 x16 控制器            │
│  14nm FinFET（中芯国际），~15 mm² │
│  ~450M 晶体管                    │
└──────────────────────────────────┘
```

### Die 2：CIM 芯片（后流片）

```
┌──────────────────────────────────┐
│  SRAM-based CIM 阵列             │
│  （存内计算，无总线瓶颈）          │
│  ↓                               │
│  DAC（数模转换）                  │
│  ↓                               │
│  ADC（模数转换）                  │
│  ↓                               │
│  数字控制接口（连主芯片）          │
│  14nm FinFET，~10 mm²            │
└──────────────────────────────────┘
```

## RTL 模块清单（已有 + 待写）

### 已有模块（可直接用或改）

| 模块 | 文件 | 状态 | 用途 |
|------|------|------|------|
| BF16 加法器 | `rtl/05_gemv/bf16_add.v` | ✅ 已验证 | GEMV 累加 |
| FP32 加法器 | `rtl/05_gemv/f32_add.v` | ✅ 已验证 | 跨组累加 |
| MXFP4 去量化 | `rtl/05_gemv/dequant.v` | ✅ 已验证 | 权重解码 |
| FP32 尺度乘法 | `rtl/05_gemv/periph_scale.v` | ✅ 已验证 | E8M0 scale |
| 跨组 MAC 累加 | `rtl/05_gemv/periph_mac.v` | ✅ 已验证 | 需改为并行阵列 |
| 预取调度器 | `rtl/06_sched/sched4.v` | ✅ 已验证 | 参考架构 |
| CIM 吞吐模型 | `tools/sim_cim_throughput.py` | ✅ 可用 | V2 设计参考 |
| DRAM 带宽模型 | `tools/sim_stream.py` | ✅ 可用 | 带宽规划 |
| 精度预算分析 | `tools/pim_precision_budget.py` | ✅ 可用 | 精度验证 |

### 待写模块（V1 必须）

| 序号 | 模块 | 优先级 | 状态 | 依赖 | 说明 |
|------|------|--------|------|------|------|
| R1 | LPDDR5X 内存控制器 | P0 | ⬜ 未开始 | — | 8 通道，460 GB/s，支持长鑫/Samsung/SK Hynix |
| R2 | PCIe Gen4 x16 控制器 | P0 | ⬜ 未开始 | — | BAR 映射、DMA、中断 |
| R3 | DMA 引擎 | P0 | ⬜ 未开始 | R1, R2 | DRAM ↔ 片上 SRAM 搬运，double-buffering |
| R4 | 命令解码器 | P0 | ⬜ 未开始 | R2 | 接收主机命令：加载权重、触发推理、读回结果 |
| R5 | GEMV 阵列顶层 | P0 | ⬜ 未开始 | 已有模块 | 115 个 bf16 MAC 并行，连接已有 add/scale/dequant |
| R6 | 片上 SRAM 控制器 | P0 | ⬜ 未开始 | R1 | 2MB SRAM buffer，读写调度 |
| R7 | 顶层模块 | P0 | ⬜ 未开始 | R1-R6 | 所有模块互联，时钟复位 |
| R8 | CIM 接口（预留） | P1 | ⬜ 未开始 | R7 | V1 不连，只留信号和时序规范 |
| R9 | 固件/微码 ROM | P1 | ⬜ 未开始 | R7 | 芯片启动、命令分发、状态机 |

### 待写模块（软件栈）

| 序号 | 模块 | 优先级 | 状态 | 依赖 | 说明 |
|------|------|--------|------|------|------|
| S1 | Linux 内核驱动 | P0 | ⬜ 未开始 | R2 | PCIe 枚举、BAR 映射、DMA、中断 |
| S2 | 用户态 SDK | P0 | ⬜ 未开始 | S1 | API：`load_weights()`、`infer()`、`read_result()` |
| S3 | 固件二进制 | P0 | ⬜ 未开始 | R9 | 芯片上的微码，运行在控制状态机上 |
| S4 | llama.cpp 适配层 | P1 | ⬜ 未开始 | S2 | 修改 ggml 后端，调用 SDK API |

### 第三方 IP（需采购/授权）

| 序号 | 模块 | 来源 | 优先级 | 状态 | 说明 |
|------|------|------|--------|------|------|
| T1 | LPDDR5X PHY + Controller | Synopsys DesignWare / Cadence | P0 | ⬜ 未开始 | 模拟 PHY，不可能自研，必须买 |
| T2 | PCIe Gen4 x16 PHY + Controller | Synopsys / Cadence | P0 | ⬜ 未开始 | 同上 |
| T3 | Standard Cell Library | 华虹/中芯国际/GlobalFoundries | P0 | ⬜ 未开始 | 28nm 工艺库 |
| T4 | SRAM Compiler | ARM Artisan / Synopsys | P0 | ⬜ 未开始 | 生成 2MB 片上 SRAM |
| T5 | PLL / Clock Gen | Synopsys / ARM | P0 | ⬜ 未开始 | 时钟生成 |
| T6 | IO Pad Library | Foundry 提供 | P0 | ⬜ 未开始 | 焊盘 IO |
| T7 | JTAG Controller | ARM CoreSight / 开源 | P1 | ⬜ 未开始 | 调试接口 |

### 待定事项（需要调研后决定）

| 序号 | 事项 | 优先级 | 状态 | 影响 | 截止时间 |
|------|------|--------|------|------|---------|
| D1 | LPDDR5X PHY 具体选哪家 | P0 | ⬜ 未开始 | 直接影响芯片面积和功耗 | RTL 开始前 |
| D2 | SRAM 容量最终定多少 | P0 | ⬜ 未开始 | 2MB 是估算，需要模型验证 | 架构设计阶段 |
| D3 | PCIe Gen4 还是 Gen5 | P1 | ⬜ 未开始 | Gen5 带宽翻倍但功耗更高 | 架构设计阶段 |
| D4 | 28nm 用哪家代工 | P0 | ⬜ 未开始 | 华虹/中芯国际/联电/格芯，影响成本和性能 | 流片前 |
| D5 | V1 是否需要 ECC | P1 | ⬜ 未开始 | ECC 增加面积但提高可靠性 | 验证阶段 |
| D6 | 驱动用 Linux 还是也要 Windows | P1 | ⬜ 未开始 | Windows 驱动开发成本更高 | 软件栈开始前 |
| D7 | 固件用 RISC-V 软核还是硬状态机 | P0 | ⬜ 未开始 | RISC-V 灵活但增加面积 | RTL 开始前 |
| D8 | CIM 接口协议具体定义 | P1 | ⬜ 未开始 | V2 要用，V1 只预留 | V1 验证前 |
| D9 | 三种容量的 BOM 成本差异 | P1 | ⬜ 未开始 | 决定主推哪个型号 | PCB 设计前 |

## 启动前（必须做完）

| # | 事项 | 怎么做 | 耗时 | 状态 | 优先级 |
|---|------|--------|------|------|--------|
| P1 | 测 EPYC 真实带宽 | `gcc -O2 stream.c -o stream && ./stream` | 1 小时 | ⬜ 未开始 | P0 |
| P2 | 修正 MAC 数 + 重算 14nm | 252→115，用 14nm 参数重算面积/功耗 | 1 天 | ⬜ 未开始 | P0 |
| P3 | 联系芯动科技问价 | 发邮件问 LPDDR5X PHY **14nm** 授权费 | 2 周 | ⬜ 未开始 | P0 |
| P4 | 找代工厂报价 | **中芯国际 14nm MPW** 价格 | 2 周 | ⬜ 未开始 | P0 |
| P5 | 找投资 | 需要 500-1000 万启动 | 持续 | ⬜ 未开始 | P0 |

**做完 P1/P3/P4 才知道项目能不能活。P5 决定要不要继续。**

## 项目阶段

| 阶段 | 任务 | 时间 | 说明 |
|------|------|------|------|
| 1 | 主芯片架构设计 | 1-2 月 | 规格定义、内存选型、GEMV 阵列、CIM 接口预留 |
| 2 | RTL 开发（内存+PCIe） | 2-3 月 | R1 LPDDR5X 控制器 + R2 PCIe + R3 DMA + R4 命令解码 |
| 3 | RTL 开发（计算+集成） | 2-3 月 | R5 GEMV 阵列顶层 + R6 SRAM 控制器 + R7 顶层模块 |
| 4 | 验证 | 2-3 月 | 每模块 testbench + 系统仿真 + FPGA 原型 |
| 5 | 物理实现 | 2-3 月 | 综合 → P&R → GDSII → 流片 |
| 6 | PCB 设计（含 CIM 焊盘） | 1-2 月 | 12 颗直焊 DRAM + CIM 预留焊盘 + PCIe |
| 7 | 系统集成 | 1-2 月 | 贴片（CIM 空焊）+ 上电调试 |
| 8 | 软件栈 | 2-3 月 | S1 驱动 + S2 SDK + S3 固件 + S4 llama.cpp 适配 |
| 9 | 商业化 | 持续 | 找客户 → 小批量 → 量产 |
| | **V1 合计** | **15-24 月** | |
| 9 | CIM 架构研究 | 3-6 月 | DAC/SRAM/ADC 设计、精度分析（与 V1 并行） |
| 10 | CIM 芯片设计 + 流片 | 9-15 月 | RTL + 模拟 + 验证 + 流片 |
| 11 | V2 系统集成 | 1-2 月 | CIM 芯片焊到 V1 PCB 上 |
| 12 | V2 软件适配 | 1-2 月 | 驱动支持 CIM 混合计算 |
| | **V2 合计** | **14-25 月**（与 V1 并行） | |

## 时间线

```
2026 Q3-Q4    2027 Q1-Q2    2027 Q3-Q4    2028 Q1-Q2    2028 Q3-Q4
    │              │              │              │              │
    ▼              ▼              ▼              ▼              ▼
┌─────────────────────────┐
│  V1：主芯片 + PCB        │
│  架构→RTL→验证→流片→量产  │
└─────────────┬───────────┘
              │ V1 验证通过
              ▼
              ┌───────────────────────┐
              │  V2：CIM 芯片          │
              │  研究→设计→流片→焊上    │
              └───────────┬───────────┘
                          │ V2 完成
                          ▼
                    性能翻倍，占领市场
```

## 启动资金

| 项 | V1（先做） | V2（后做，追加） |
|---|---|---|
| 流片（28nm，MPW） | 30-80 万 | 30-80 万 |
| FPGA 开发板 | 1-3 万 | — |
| PCB + 贴片（12 颗 DRAM 直焊 + CIM 焊盘预留） | 8-15 万 | 3-5 万（CIM 贴片） |
| 服务器（仿真） | 5-10 万 | 5-10 万 |
| 团队 | 100-200 万 | 150-300 万 |
| **合计** | **150-300 万** | **190-400 万** |

## PIM 精度模拟结果（2026-08-20，`tools/pim_sim_full.py`，733 tensor / 472 有效）

```
mxfp4            p50(max_err) = 3.1e-1    ← cell 表示饱和（8 档电平不够）
int8_blk32       p50           = 1.1e-2    ← cell + DAC10 + ADC12 + noise 全部叠加
int8_blk256      p50           = 1.4e-2
int6_blk32       p50           = 3.4e-2    ← 边界可用
dac_adc_only     p50           = 2.9e-3    ← 外围量化不是瓶颈
cell_only_int8   p50           = 8.1e-3    ← 纯 cell 量化误差
```

结论：
1. MXFP4 精度不可用（p50=31%），DAC/ADC 噪声在此之上不叠加
2. 8-bit uniform cell (blk32) 精度可行（p50=1.1%），源量化类型差异 <2×
3. 6-bit cell 边界可用（p50=3.4%）
4. PIM 精度不是瓶颈，带宽才是。PIM 硬件方向仍取决于能否做足够高的带宽

## 推广路径

| 步骤 | V1（低配） | V2（高配） |
|------|-----------|-----------|
| 1 | 第一片自己用，验证能跑 LLM | CIM 研究阶段，发论文/申请专利 |
| 2 | 第二片送大佬/投资人 | V1 客户升级：焊上 CIM 芯片 |
| 3 | 等订单，小批量生产 | V2 量产，性能翻倍 |
| 4 | 量产，占领市场 | 新客户直接买 V2 |

## 风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| 内存供货紧张 | 芯片没内存可用 | 长鑫 CXMT 国产首选 + 国际备选 |
| V1 流片失败 | 费用打水漂 | 多轮仿真 + MPW |
| 首个客户找不到 | 无法变现 | 先做 FPGA 原型验证 |
| CIM 芯片精度不够 | V2 计算结果不准 | 校准算法 + V1 仍可用 |
| CIM 芯片良率低 | V2 成本高 | V1 已经赚钱，可以承受 |
| 竞品抢先 | 市场被占 | V1 聚焦成本，V2 聚焦带宽 |
| 软件生态 | 用户不愿切换 | llama.cpp 适配 |
| DRAM 直焊不可升级 | 客户容量需求变化 | 提供 24G/48G/96G 三种型号 |

## 技术对标

| 指标 | RTX 3090 | H100 | V1（低配） | V2（高配） |
|------|----------|------|-----------|-----------|
| 产品形态 | 加速卡 | 加速卡 | **加速卡** | **加速卡+** |
| 工艺 | 8nm | 4nm | **14nm FinFET** | 14nm FinFET |
| 内存 | 24GB GDDR6X | 80GB HBM3 | **48GB LPDDR5X** | 同 V1 |
| 内存带宽 | 936 GB/s | 3.35 TB/s | **550 GB/s** | 更高 |
| 功耗 | 350W | 700W | **~40W** | ~55W |
| 成本 | $1000 | $30000+ | **$250** | $350 |
| LLM tok/s | ~30-40 | ~60-80 | **25-30** | 50-100+ |
| 竞争对手 | NVIDIA | NVIDIA | NVIDIA/AMD | 同上 |

## 核心结论

1. **全直焊 LPDDR5X**：460 GB/s 带宽，不用插槽，最简单最快最便宜。
2. **同一块 PCB 做高低配**：V1 空焊盘，V2 贴 CIM 芯片。PCB 只设计一次。
3. **V1 是生存**：纯数字，简单，能活。用成熟技术验证商业价值。
4. **V2 是突破**：CIM 芯片独立设计、独立流片，失败不影响 V1。
5. **三种容量可选**：24G / 48G / 96G，买时选好。
6. **已有 RTL 可复用**：BF16/FP32/add/scale/MAC 已验证，省 2-3 月开发时间。
7. **启动资金**：V1 需要 150-300 万 RMB，V2 追加 190-400 万 RMB。
